#!groovy
//noinspection GroovyUnusedAssignment
@Library("Infrastructure@removesonar")
def type = "nodejs"
def product = "ccpay-functions"
def component = "node"

withPipeline(type, product, component) {
  installCharts()
  disableLegacyDeployment()
  enableAksStagingDeployment()
  nonServiceApp()
}
